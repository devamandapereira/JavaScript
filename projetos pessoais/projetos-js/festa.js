let pessoa = 'Amanda' 

if (pessoa == 'Amanda') {
console.log (`Olá, ${pessoa}. Seu nome está na lista. Seja bem-vindo!`)}
else {
    console.log (`Olá, ${pessoa}. Seu nome não está na lista. Você não pode entrar.`)
}